# 交互
关于k8s版本指定的问题 - 已解决
	- 使用最新的版本
  - 待完善(?)- 暂定

是否需要提前获取镜像文件  - yes
	- 需要调整 get_images函数
是否使用本地离线镜像文件 - no
  - 需要调整 get_images函数 - 完善离线镜像逻辑



旧版集群清理失误

工作节点传递主机列表是有问题的-目前包括了master节点a
- 已解决，add_node函数的 第二个参数没有用
- 子脚本中修改一个变量，如果该变量与父脚本中的环境变量同名，
  那么，父脚本中的环境变量会被修改，导致其他应用父脚本环境变量的子函数，出现异常

后续：
	关于在线镜像获取 和 离线镜像获取 拆分成两个小函数
  
  关于等待的时间显示 -- 最好有个过程内容，(最后不显示过程信息)- 已解决
